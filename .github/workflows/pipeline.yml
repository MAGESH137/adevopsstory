name: Deploy with Secrets

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use a custom secret
        run: echo "Deploying to ${{ secrets.DEPLOY_HOST }}"
